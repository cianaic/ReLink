rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{userId} {
      // Allow users to read and write their own data
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
    match /activities/{activityId} {
      // Allow users to read all activities but only write their own
      allow read: if request.auth != null;
      allow create: if request.auth != null && request.resource.data.userId == request.auth.uid;
      allow update, delete: if request.auth != null && resource.data.userId == request.auth.uid;
    }
    match /relinks/{document} {
      // Allow authenticated users to read all relinks
      allow read: if request.auth != null;
      // Allow users to create relinks if they haven't exceeded monthly limit
      allow create: if request.auth != null 
        && request.resource.data.userId == request.auth.uid;
      // Allow users to update and delete their own relinks
      allow update, delete: if request.auth != null 
        && resource.data.userId == request.auth.uid;
    }
    match /shared_relinks/{document} {
      // Allow authenticated users to read all shared links
      allow read: if request.auth != null;
      // Allow users to create shared links
      allow create: if request.auth != null 
        && request.resource.data.userId == request.auth.uid;
      // Allow users to update and delete their own shared links
      allow update, delete: if request.auth != null 
        && resource.data.userId == request.auth.uid;
    }
  }
}